import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.marco.app',
  appName: 'mobile-angular-capacitor-app',
  webDir: 'dist/mobile-angular-capacitor-app/browser'
};

export default config;
